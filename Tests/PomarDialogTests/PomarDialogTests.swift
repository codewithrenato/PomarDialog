import XCTest
@testable import PomarDialog

final class PomarDialogTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(PomarDialog().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
